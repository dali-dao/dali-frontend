import { useStyles } from "./Style";

function Footer() {
    const classes = useStyles();

    return (
      <div className={classes.footer}>
          Footer
      </div>
    );
  }
  
  export default Footer;
  